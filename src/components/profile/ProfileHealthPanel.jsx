import React from 'react';
import { ProfileSetupRow } from './ProfileSetupRow';

/** @deprecated Prefer ProfileSetupRow — thin alias for existing imports. */
export function ProfileHealthPanel(props) {
  return <ProfileSetupRow {...props} />;
}
