/**
 * Semantic alias for wide tabular content on small screens.
 * Prefer wrapping `<table>` trees so horizontal scroll stays local (see `AppTableWrap`).
 */
export { AppTableWrap as ResponsiveTableShell } from '../ui/AppDataTable';
