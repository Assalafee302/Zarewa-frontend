# Zarewa ERP — internal staff application, not a public site.
#
# Every route behind this host is an authenticated desk for mill staff in Kaduna and
# Yola. There is no search traffic to win here, and indexing the app would publish the
# login page and route structure to anyone scanning. Crawling is refused outright.
#
# If a public marketing site is ever wanted, it belongs on its own host with its own
# robots.txt — not on the application staff log into.

User-agent: *
Disallow: /
